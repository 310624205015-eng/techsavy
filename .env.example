# Vite / Frontend environment variables
VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/REPLACE_WITH_YOUR_ID/exec
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Notes:
# - Set VITE_APPS_SCRIPT_URL to the deployed Apps Script Web App URL (must end with /exec).
# - Do NOT store SUPABASE_SERVICE_KEY in frontend env files. If you need Apps Script to
#   call Supabase directly (exportFromSupabase), add SUPABASE_URL and SUPABASE_SERVICE_KEY
#   as Script Properties within the Apps Script project (in the Apps Script editor: Project Settings -> Script properties).